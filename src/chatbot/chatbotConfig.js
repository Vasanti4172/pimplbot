import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  botName: "SkinBot",
  initialMessages: [createChatBotMessage("Hello! Select a facial area and choose a skin issue.")],
};

export default config;
